name: class_type_emission
description: >-
  M9-M fixture: a bounded project-class TYPE reaches generated TypeScript honestly — a real, named,
  type-only interface instead of `unknown` — without implying construction, field access, getter reads,
  or method calls, none of which this generator implements yet
  (docs/m9/m9m-project-class-type-emission-and-reachability.md).
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
