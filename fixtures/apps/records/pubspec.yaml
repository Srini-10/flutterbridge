name: records
description: >-
  M13 (ADR-0069): Dart 3 records (positional, named, mixed, nullable, equality) and record/list/map patterns in declarations,
  if-case and switches. Run by Flutter and as the generated component, compared.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
