name: interaction_e2e
description: >-
  M13 browser proof (ADR-0070, ADR-0071): gestures (tap family, hover, focus, keyboard) and constraints (LayoutBuilder) in a
  real browser, production and development.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
