name: project_statics_refusal
description: >-
  M12: a mutable static field and a mutable top-level variable are refused by name (module state would be shared by every
  request in a server process).
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
