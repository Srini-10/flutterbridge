name: patterns
description: >-
  M12 (ADR-0065): Dart 3 patterns — switch expressions and pattern cases with object, constant, relational, or/and and binding
  patterns and when-guards. Run by Flutter and as the generated component, compared.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
