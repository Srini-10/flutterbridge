name: widget_constructors
description: >-
  M12 (ADR-0063): widget constructors — an initializer list that sets a constant, a named constructor, a factory. Run by Flutter and as
  the generated component, compared.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
