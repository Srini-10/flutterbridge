name: layout
description: >-
  M13 (ADR-0071): LayoutBuilder and constraints — a builder reading its own maxWidth/maxHeight, rebuilt on resize, nested, responsive.
  Run by Flutter and as the generated component, compared.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
