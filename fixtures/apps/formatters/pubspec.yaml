name: formatters
description: >-
  M14 (ADR-0074): TextInputFormatters on TextField and TextFormField — digitsOnly, LengthLimitingTextInputFormatter, both together, a rejected
  edit not reported as a change — compared with Flutter.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
