# Compatibility report — app_b (`shop_bridge`)

> Generated by the M0-T6 spike. **Analysis only** — no UIR, no code, no rewriting.

## Compatibility: Low Compatibility

The application depends on capabilities that do not exist in a browser at all — web-incompatible imports (dart:isolate, dart:ffi, dart:io), platform channels and isolates. These are not gaps in the compiler; they are gaps in the target platform. Converting this application means a human first decides what those capabilities become on the web. The UI layer itself may still convert well — see the supported/partial lists.

## Summary

| | |
| --- | --- |
| Project | `shop_bridge` |
| Flutter | 3.41.5 |
| Dart | 3.11.3 |
| Dart files | 10 |
| Lines of code | 505 |
| Widget instantiations | 62 |
| Distinct widget types | 32 |
| Stateless widgets | 1 |
| Stateful widgets | 3 |
| State classes | 3 |
| Stores or controllers | 1 |
| Custom painters | 1 |
| Inherited widgets | 0 |
| Async methods | 7 |
| Await expressions | 7 |
| Set state calls | 3 |
| Named route entries | 0 |
| Has on generate route | true |
| Network call sites | 2 |
| User defined components | 4 |
| Unsupported constructs | 13 |
| Unknown constructs | 0 |

## Application components

Widgets declared by this application. These are what the compiler generates; they are not framework constructs to be mapped.

- `CatalogScreen`
- `CheckoutScreen`
- `ProductScreen`
- `ShopApp`

## Supported features

Inside the frozen MVP subset (Blueprint §5.2).

- `AppBar`
- `Card`
- `Center`
- `CircularProgressIndicator`
- `Column`
- `Container`
- `ElevatedButton`
- `Expanded`
- `FutureBuilder`
- `Icon`
- `InkWell`
- `MaterialApp`
- `Padding`
- `Row`
- `Scaffold`
- `SizedBox`
- `Spacer`
- `Text`

## Partially supported

Recognised, has a plausible web mapping, but outside the frozen MVP — planned, not promised.

- `FloatingActionButton`
- `Form`
- `GridView.builder`
- `ListenableBuilder`
- `SingleChildScrollView`
- `StreamBuilder`
- `TextFormField`

## Unsupported features

Outside the MVP with no straightforward mapping. Not solved here.

- `AnimationController`
- `CustomPaint`
- `CustomPainter subclass`
- `FadeTransition`
- `Hero`
- `Isolate.spawn`
- `TickerProvider mixin (explicit animation)`
- `compute() (isolate)`
- `import dart:ffi`
- `import dart:io`
- `import dart:isolate`
- `platform channel (MethodChannel)`
- `platform channel invocation`

## Unknown constructs

Widgets the tool does not recognise at all. Every entry here is a gap in the catalog, and a place a user will need guidance.

_None._

## Navigation

| Pattern | Sites |
| --- | --- |
| `Navigator.pop` | `lib/screens/checkout_screen.dart:70`, `lib/screens/product_screen.dart:69` |
| `Navigator.pushNamed` | `lib/screens/catalog_screen.dart:103`, `lib/screens/catalog_screen.dart:66` |
| `onGenerateRoute` | `lib/main.dart:30` |

## Network

| Call | Sites |
| --- | --- |
| `http.GET` | `lib/services/api_client.dart:14` |
| `http.POST` | `lib/services/api_client.dart:25` |

## Warnings

### W02 — A non-primitive object is passed as a route argument

**Why.** Flutter passes a live Dart object through `Navigator.pushNamed(arguments:)`. A URL route can carry an id, not an object graph.

**Impact.** The destination screen must re-fetch or re-derive the object from an identifier, which changes its data-loading shape.

**Likely compiler behaviour.** Lower the argument to a route parameter (an id) plus a data load on the destination route; flag when the object cannot be reduced to an identifier.

**Owner.** normalization + generator

**Where.** `lib/screens/catalog_screen.dart:66`

### W03 — Module-level mutable store singleton

**Why.** A top-level `ChangeNotifier` instance is process-global. In Flutter that is one user. On a Next.js server the module is shared across *all* requests.

**Impact.** If such a store is touched during SSR, one user can see another user's cart. This is a correctness and privacy bug, not a styling one.

**Likely compiler behaviour.** Emit the store as a per-request/provider-scoped instance and mark the component tree that touches it as client-only.

**Owner.** generator (RSC boundary) + runtime (state runtime)

**Where.** `lib/state/cart_store.dart:28`

### W04 — Platform channels

**Why.** MethodChannel/EventChannel call into native Android/iOS code. There is no native side on the web.

**Impact.** Any feature behind the channel cannot be converted at all; it needs a web implementation written by a human (an HTTP endpoint, a browser API, or a stub).

**Likely compiler behaviour.** Emit an Opaque node and require an override; never guess a substitute.

**Owner.** analyzer (detect) + documentation (override workflow)

**Where.** `lib/services/native_bridge.dart:10`, `lib/services/native_bridge.dart:7`

### W05 — Isolates / compute()

**Why.** Isolates are Dart's concurrency model. The browser equivalent (Web Workers) has a different memory and messaging model.

**Impact.** Work offloaded from the UI thread would run on the main thread instead, or must be rewritten against Web Workers.

**Likely compiler behaviour.** Out of scope for the MVP: flag as unsupported and require an override.

**Owner.** analyzer (detect) + documentation

**Where.** `lib/services/heavy_compute.dart:15`, `lib/services/heavy_compute.dart:7`

### W06 — CustomPainter / CustomPaint

**Why.** Imperative canvas drawing. Spec §7 names this as the automation boundary.

**Impact.** The painted widget cannot be generated; it needs a hand-written React component.

**Likely compiler behaviour.** Emit an Opaque node keyed by a stable anchor and route it to the override system, so the human writes it once and regeneration preserves it.

**Owner.** generator (override system)

**Where.** `lib/screens/product_screen.dart:55`, `lib/widgets/rating_painter.dart:3`

### W07 — Explicit animation (AnimationController / TickerProvider)

**Why.** Explicit animation choreography is outside the MVP; only implicit animations are planned to map cleanly.

**Impact.** Transitions driven by a controller will not be reproduced.

**Likely compiler behaviour.** The animation runtime handles the implicit cases; a controller timeline flags for an override.

**Owner.** runtime (animation engine)

**Where.** `lib/screens/product_screen.dart:16`, `lib/screens/product_screen.dart:25`

### W08 — Web-incompatible import: dart:isolate

**Why.** Isolates have no direct React analogue.

**Impact.** Any code path reaching this import cannot run in a browser, converted or not.

**Likely compiler behaviour.** Refuse to convert the enclosing unit; require an override or a web-specific implementation.

**Owner.** analyzer (detect) + bridge_lints (prevent, for new projects)

**Where.** `lib/services/heavy_compute.dart:1`

### W08 — Web-incompatible import: dart:ffi

**Why.** dart:ffi is native interop; there is no web or React analogue.

**Impact.** Any code path reaching this import cannot run in a browser, converted or not.

**Likely compiler behaviour.** Refuse to convert the enclosing unit; require an override or a web-specific implementation.

**Owner.** analyzer (detect) + bridge_lints (prevent, for new projects)

**Where.** `lib/services/native_bridge.dart:1`

### W08 — Web-incompatible import: dart:io

**Why.** dart:io is not available on the web at all.

**Impact.** Any code path reaching this import cannot run in a browser, converted or not.

**Likely compiler behaviour.** Refuse to convert the enclosing unit; require an override or a web-specific implementation.

**Owner.** analyzer (detect) + bridge_lints (prevent, for new projects)

**Where.** `lib/services/native_bridge.dart:2`

### W09 — StreamBuilder

**Why.** Streams are outside the frozen MVP (only FutureBuilder is in).

**Impact.** Live-updating UI will not be generated at MVP.

**Likely compiler behaviour.** Planned: map to a subscription (useSyncExternalStore / query subscription). Recognised, not yet supported.

**Owner.** normalization + runtime (state runtime)

**Where.** `lib/screens/catalog_screen.dart:84`

