name: m0_compat_report
description: >-
  M0-T6 SPIKE. Throwaway compatibility-report prototype: analyzes a Flutter project and reports what
  is supported / partially supported / unsupported. Generates NO code and transforms nothing.
publish_to: 'none'
version: 0.0.0

environment:
  sdk: ^3.11.0

dependencies:
  analyzer: ^14.0.0
  path: ^1.9.0
