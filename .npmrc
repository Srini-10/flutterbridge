engine-strict=true
# Keep the dependency graph honest: a package must declare what it imports (Spec §1.2 is
# meaningless if phantom hoisted deps are importable).
hoist=false
strict-peer-dependencies=false
