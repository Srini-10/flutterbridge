name: counter
description: The smallest FlutterBridge example — a Flutter app that compiles to React.
publish_to: none
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
