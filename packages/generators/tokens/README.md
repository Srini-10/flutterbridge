# packages/generators/tokens — reserved

@bridge/gen-tokens — design realm (DTCG tokens; Figma sync feeds off this). Listed in Spec v2.0 §1.1 but not scheduled in any Blueprint milestone table.

**Milestone: unscheduled — do not implement until it is scheduled.** This directory exists because Spec v2.0 §1.1 / the Implementation
Blueprint §1.1 define the tree up front. It carries no code, no `package.json`, and is not a
workspace member until its milestone is reached (Blueprint §5.2: never implement a blocklisted item
before its milestone).
