// @bridge/gen-react — Target #1 — Next.js generator: scaffolder, lower-signals, rsc-split, UINode emitters, route emitter.
//
// BRIDGE-STUB(M2): public surface — reactGenerator: TargetGenerator. See Blueprint §3 M2-T16..T20.
//
// The package exists from M0 so that the architecture rules (Spec §1.2) are enforced from the
// first commit, and so that dependents can be written against a real module boundary.

export {};
