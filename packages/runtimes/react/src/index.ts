// @bridge/runtime-react — Runtime compatibility kit: layout, theme, nav, state engines (animation engine post-MVP).
//
// BRIDGE-STUB(M2): sub-path entrypoints /layout, /theme, /nav, /state. Ships to end-user apps and never depends on compiler packages (Spec §1.2 rule 4). See Blueprint §3 M2-T12..T15.
//
// The package exists from M0 so that the architecture rules (Spec §1.2) are enforced from the
// first commit, and so that dependents can be written against a real module boundary.

export {};
