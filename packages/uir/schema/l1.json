{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://flutterbridge.dev/uir/l1.json",
  "title": "UIR L1 — semantic",
  "description": "Declarations, statements and expressions: the Dart the analyzer resolved. Generators that only need routes or components never load this layer.",
  "x-uir-layer": "L1",
  "$defs": {
    "ConstructibleConstructor": {
      "type": "object",
      "description": "One of a class's own constructors, proven safely equivalent to a plain object literal's own construction (ADR-0036/ADR-0037).",
      "additionalProperties": false,
      "required": [
        "kind",
        "fields"
      ],
      "properties": {
        "name": {
          "type": "string",
          "description": "The constructor's own name (e.g. `named`, for `Model.named(...)`). Absent for the unnamed constructor."
        },
        "kind": {
          "type": "string",
          "enum": [
            "positional",
            "named"
          ],
          "description": "Whether this constructor's own field-formal parameters are all required-positional (`fields` in parameter/call-argument order) or all required-named (`fields` in parameter declaration order — a generator matches each one to a call's own `namedArgs` by the field's own name, which Dart's own grammar guarantees equals the field-formal parameter's declared external label). A constructor mixing required-positional and required-named field-formals is excluded entirely (ADR-0037) — narrower than Dart itself allows, kept out of the first subset deliberately."
        },
        "fields": {
          "type": "array",
          "items": {
            "$ref": "shared.json#/$defs/NodeId"
          },
          "description": "The `logic.FieldDecl` each of this constructor's own required field-formal parameters initializes. For `kind: \"positional\"`, index-for-index with a call's own positional `args`. For `kind: \"named\"`, one entry per required named field-formal, in declaration order — order here carries no calling significance, only determinism; a generator resolves each field's own value from a call's own `namedArgs`, keyed by the field's own name."
        }
      }
    },
    "ParamDecl": {
      "type": "object",
      "description": "A parameter of a function, method, or widget constructor.",
      "additionalProperties": false,
      "required": [
        "name",
        "type"
      ],
      "properties": {
        "name": {
          "type": "string",
          "description": "Parameter name."
        },
        "type": {
          "$ref": "shared.json#/$defs/TypeRef",
          "description": "Resolved type."
        },
        "required": {
          "type": "boolean",
          "description": "Whether the parameter is required."
        },
        "named": {
          "type": "boolean",
          "description": "Whether the parameter is named rather than positional."
        },
        "defaultValue": {
          "$ref": "l1.json#/$defs/Expr",
          "description": "Default value, if any."
        },
        "initializesField": {
          "type": "string",
          "description": "For an initializing formal (`this.x`), the name of the field it initializes. Present only then (M12)."
        },
        "isSuper": {
          "type": "boolean",
          "description": "Whether this is a super parameter (`super.x`), forwarded to the superclass constructor under the same name. Present only when true (M12)."
        }
      }
    },
    "SwitchCase": {
      "type": "object",
      "description": "One case of a switch statement.",
      "additionalProperties": false,
      "required": [
        "body"
      ],
      "properties": {
        "test": {
          "$ref": "l1.json#/$defs/Expr",
          "description": "The value matched. Absent for the default case."
        },
        "body": {
          "type": "array",
          "items": {
            "$ref": "l1.json#/$defs/Stmt",
            "description": "A statement in the case body."
          },
          "description": "Body, in order."
        },
        "pattern": {
          "$ref": "#/$defs/Pattern",
          "description": "The pattern a Dart 3 `case` matches, when it is not a plain constant (`test`)."
        },
        "guard": {
          "$ref": "l1.json#/$defs/Expr",
          "description": "The `when` clause of a pattern case."
        }
      }
    },
    "CatchClause": {
      "type": "object",
      "description": "One catch clause of a try statement.",
      "additionalProperties": false,
      "required": [
        "body"
      ],
      "properties": {
        "exceptionType": {
          "$ref": "shared.json#/$defs/TypeRef",
          "description": "The type caught, if narrowed."
        },
        "exceptionName": {
          "type": "string",
          "description": "The bound exception variable, by name only. `exceptionDecl` (ADR-28, amended M8-S) is the same binding's own declaration-tier identity — this field is retained unchanged, for description only."
        },
        "exceptionDecl": {
          "$ref": "#/$defs/VarDecl",
          "description": "The exception binding's own declaration (ADR-28, amended M8-S) — a real, declaration-tier `logic.VarDecl`, with no `initializer` (the runtime binds it, not an expression). A `logic.Ref` inside the clause body targets this node's own id, the same way any other local variable's read already does. Absent exactly when `exceptionName` is (no exception parameter at all)."
        },
        "stackTraceName": {
          "type": "string",
          "description": "The bound stack-trace variable. Unlike `exceptionName`, this has no declaration-tier counterpart yet — a stack-trace binding has no JavaScript catch-parameter equivalent (the nearest analogue, `error.stack`, is a property read, not a second binding), a materially different mapping question this amendment does not resolve."
        },
        "body": {
          "$ref": "#/$defs/Block",
          "description": "The clause body."
        }
      }
    },
    "NavigateAction": {
      "description": "What a `logic.Navigate` does.\n\nNamed for the **effect on the navigation stack**, not for the Flutter API that produced it: a `go_router` `context.go` and a `Navigator.pushNamed` are both `push`, and a generator lowers the effect rather than recognising a package. ADR-0025 §5 is why — the M6-D corpus found zero `go_router` in two production applications after C1 recorded it as dominant, so which package is popular is not something to build a vocabulary on.",
      "enum": [
        "push",
        "replace",
        "pop",
        "popUntil"
      ],
      "x-uir-enum-docs": {
        "push": "A new entry on the stack. `Navigator.push`, `pushNamed`, and every route overlay — a dialog, modal sheet or menu pushes a `Route` (ADR-0024 cites the SDK).",
        "replace": "The current entry is replaced. `pushReplacement`, `pushReplacementNamed`.",
        "pop": "The top entry is removed. `pop`, `maybePop`.",
        "popUntil": "Entries are removed until a predicate holds. The predicate is **not** modelled; a generator that cannot express one must refuse rather than approximate."
      }
    },
    "IntrinsicKind": {
      "description": "The vocabulary of framework-provided facts `logic.Intrinsic` can represent (ADR-0026). Deliberately target-neutral — a name for what the fact *means*, never the framework API that answers it today, so a non-Flutter frontend could produce the same node for its own equivalent concept.",
      "enum": [
        "componentMounted",
        "contextMounted"
      ],
      "x-uir-enum-docs": {
        "componentMounted": "Whether the enclosing component instance is still part of the live tree. Nullary — asks about the component this expression is lexically inside; `logic.Intrinsic.operand` is absent. Flutter: `State.mounted`.",
        "contextMounted": "Whether the element behind a specific context value is still part of the live tree. Takes `operand` — the context value — because it asks about whatever was passed in, which in the common real case is not the enclosing component (a free function taking a `BuildContext` parameter has none). Flutter: `BuildContext.mounted`."
      }
    },
    "AssignmentOperator": {
      "description": "How an assignment combines its target with its value (Spec v2.2 §A10).\n\nAn enum, not a free-form string: a generator that receives an operator it does not know must fail to compile rather than silently emit the wrong arithmetic. `Binary.operator` is a string because a binary operator is pure — the worst a wrong one does is compute the wrong number. A wrong assignment operator writes the wrong value to state.",
      "enum": [
        "assign",
        "addAssign",
        "subtractAssign",
        "multiplyAssign",
        "divideAssign",
        "truncatingDivideAssign",
        "moduloAssign",
        "ifNullAssign",
        "bitAndAssign",
        "bitOrAssign",
        "bitXorAssign",
        "shiftLeftAssign",
        "shiftRightAssign",
        "unsignedShiftRightAssign",
        "increment",
        "decrement"
      ],
      "x-uir-enum-docs": {
        "assign": "`=` — replace the target's value.",
        "addAssign": "`+=`",
        "subtractAssign": "`-=`",
        "multiplyAssign": "`*=`",
        "divideAssign": "`/=`",
        "truncatingDivideAssign": "`~/=` — Dart's integer division. JavaScript has no operator for it, so a generator must emit `Math.trunc(a / b)`; conflating it with `/=` silently changes the result type.",
        "moduloAssign": "`%=` — Dart's modulo is always non-negative for a positive divisor; JavaScript's `%` is not. A generator must not assume they agree.",
        "ifNullAssign": "`??=` — assign only if the target is null.",
        "bitAndAssign": "`&=`",
        "bitOrAssign": "`|=`",
        "bitXorAssign": "`^=`",
        "shiftLeftAssign": "`<<=`",
        "shiftRightAssign": "`>>=`",
        "unsignedShiftRightAssign": "`>>>=`",
        "increment": "`++` — `value` is absent.",
        "decrement": "`--` — `value` is absent."
      }
    },
    "Lit": {
      "description": "A literal: a number, string, boolean, or null.",
      "x-uir-kind": "logic.Lit",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Lit",
              "description": "Discriminant."
            },
            "value": {
              "x-uir-json": true,
              "description": "The literal value."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            }
          }
        }
      ]
    },
    "Ref": {
      "description": "A reference to a declared name — a local, a parameter, a field, or a signal.",
      "x-uir-kind": "logic.Ref",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "name",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Ref",
              "description": "Discriminant."
            },
            "name": {
              "type": "string",
              "description": "The name referenced."
            },
            "target": {
              "$ref": "shared.json#/$defs/NodeId",
              "description": "The declaration referred to, when it is in the program."
            },
            "library": {
              "type": "string",
              "description": "The library an external name comes from when it is not declared in the program: a Dart SDK top-level function, constant or static member (`dart:core`), or a `const` top-level variable of a package the program does not extract. The generator lowers a known SDK one (`identical`, `Object.hash`, `double.infinity`), treats a package constant as an opaque canonical token (identity and equality only), and refuses the rest."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            }
          }
        }
      ]
    },
    "Call": {
      "description": "A call to a function.",
      "x-uir-kind": "logic.Call",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "callee",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Call",
              "description": "Discriminant."
            },
            "callee": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The function called."
            },
            "args": {
              "type": "array",
              "items": {
                "$ref": "l1.json#/$defs/Expr",
                "description": "An argument, in source order."
              },
              "description": "Positional arguments, in order."
            },
            "namedArgs": {
              "type": "object",
              "additionalProperties": {
                "$ref": "l1.json#/$defs/Expr",
                "description": "A named argument."
              },
              "description": "Named arguments, keyed by parameter name."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved return type."
            }
          }
        }
      ]
    },
    "MethodCall": {
      "description": "A call to a method on a receiver.",
      "x-uir-kind": "logic.MethodCall",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "receiver",
            "method",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.MethodCall",
              "description": "Discriminant."
            },
            "receiver": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The receiver."
            },
            "method": {
              "type": "string",
              "description": "The method name."
            },
            "target": {
              "$ref": "shared.json#/$defs/NodeId",
              "description": "The store member (sig.Action) this resolves to, when the receiver's resolved type is a declared store (ADR-27). Absent for an ordinary method call."
            },
            "args": {
              "type": "array",
              "items": {
                "$ref": "l1.json#/$defs/Expr",
                "description": "An argument."
              },
              "description": "Positional arguments, in order."
            },
            "namedArgs": {
              "type": "object",
              "additionalProperties": {
                "$ref": "l1.json#/$defs/Expr",
                "description": "A named argument."
              },
              "description": "Named arguments."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved return type."
            },
            "extensionTarget": {
              "$ref": "shared.json#/$defs/NodeId",
              "description": "The extension member (`logic.FunctionDecl` with `extensionOn`) this call or access resolves to (ADR-0068). The receiver is the extension's `this`."
            }
          }
        }
      ]
    },
    "PropertyAccess": {
      "description": "Reading a property of a value.",
      "x-uir-kind": "logic.PropertyAccess",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "receiver",
            "property",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.PropertyAccess",
              "description": "Discriminant."
            },
            "receiver": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The receiver."
            },
            "property": {
              "type": "string",
              "description": "The property name."
            },
            "target": {
              "$ref": "shared.json#/$defs/NodeId",
              "description": "The store member (sig.Signal/sig.Derived) this resolves to, when the receiver's resolved type is a declared store (ADR-27). Absent for an ordinary property access."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            },
            "extensionTarget": {
              "$ref": "shared.json#/$defs/NodeId",
              "description": "The extension member (`logic.FunctionDecl` with `extensionOn`) this call or access resolves to (ADR-0068). The receiver is the extension's `this`."
            }
          }
        }
      ]
    },
    "Binary": {
      "description": "A binary operation, e.g. `a + b`.",
      "x-uir-kind": "logic.Binary",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "operator",
            "left",
            "right",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Binary",
              "description": "Discriminant."
            },
            "operator": {
              "type": "string",
              "description": "The operator, e.g. `+`."
            },
            "left": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "Left operand."
            },
            "right": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "Right operand."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            }
          }
        }
      ]
    },
    "Unary": {
      "description": "A unary operation, e.g. `!a`.",
      "x-uir-kind": "logic.Unary",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "operator",
            "operand",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Unary",
              "description": "Discriminant."
            },
            "operator": {
              "type": "string",
              "description": "The operator, e.g. `!`."
            },
            "operand": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The operand."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            }
          }
        }
      ]
    },
    "Assign": {
      "description": "A write to a target: `a = b`, `a += b`, `a++`.\n\nAdded in v2.2 (§A10). Before it, the `Expr` union had no assignment at all, so every `setState` body — the one thing a Flutter→React compiler exists to translate — could only be carried as `OpaqueExpr`, i.e. a Dart source string no generator can compile. A census of the C1 corpus found 401 assignments across compass_app and wonderous, of which 210 were inside a `State` or `ChangeNotifier`; in our own fixture, *every* assignment was a state mutation.\n\nDistinct from `sig.Action.writes`, and not replaceable by it: `writes` is a data-flow summary (*which* signals change), while this is program semantics (*what they become*). Both are required.",
      "x-uir-kind": "logic.Assign",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "target",
            "operator",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Assign",
              "description": "Discriminant."
            },
            "target": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The place written to — a `Ref`, a `PropertyAccess`, or an index. It is an lvalue, which is why this cannot be a `Binary`: `Binary.left` is a value, and a generator reading one as the other emits a comparison where a write was meant."
            },
            "operator": {
              "$ref": "l1.json#/$defs/AssignmentOperator",
              "description": "How the value combines with the target."
            },
            "value": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The value assigned. Absent for `increment` and `decrement`, which have no operand."
            },
            "isPostfix": {
              "type": "boolean",
              "description": "For `increment`/`decrement`: whether the operator followed its target (`i++`) rather than preceded it (`++i`). The two differ only when the expression's value is consumed — `list[i++]` — but they do differ, and dropping the distinction would silently generate the wrong index."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type of the expression — the value written. Required, because every other member of the `Expr` union requires it and a consumer may rely on that."
            }
          }
        }
      ]
    },
    "Conditional": {
      "description": "A ternary conditional, e.g. `a ? b : c`.",
      "x-uir-kind": "logic.Conditional",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "test",
            "then",
            "otherwise",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Conditional",
              "description": "Discriminant."
            },
            "test": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The condition."
            },
            "then": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "Value when true."
            },
            "otherwise": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "Value when false."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            }
          }
        }
      ]
    },
    "Lambda": {
      "description": "An anonymous function.",
      "x-uir-kind": "logic.Lambda",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "body",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Lambda",
              "description": "Discriminant."
            },
            "params": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ParamDecl",
                "description": "A parameter."
              },
              "description": "Parameters, in order."
            },
            "body": {
              "type": "array",
              "items": {
                "$ref": "l1.json#/$defs/Stmt",
                "description": "A statement."
              },
              "description": "Body, in order."
            },
            "isAsync": {
              "type": "boolean",
              "description": "Whether the lambda is async."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved function type."
            }
          }
        }
      ]
    },
    "Await": {
      "description": "An `await` expression.",
      "x-uir-kind": "logic.Await",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "operand",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Await",
              "description": "Discriminant."
            },
            "operand": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The awaited expression."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            }
          }
        }
      ]
    },
    "ListLit": {
      "description": "A list literal.",
      "x-uir-kind": "logic.ListLit",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.ListLit",
              "description": "Discriminant."
            },
            "elements": {
              "type": "array",
              "items": {
                "$ref": "l1.json#/$defs/Expr",
                "description": "An element."
              },
              "description": "Elements, in order. Order is semantic."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            }
          }
        }
      ]
    },
    "MapLit": {
      "description": "A map literal.",
      "x-uir-kind": "logic.MapLit",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.MapLit",
              "description": "Discriminant."
            },
            "keys": {
              "type": "array",
              "items": {
                "$ref": "l1.json#/$defs/Expr",
                "description": "A key."
              },
              "description": "Keys, in order, paired positionally with `values`."
            },
            "values": {
              "type": "array",
              "items": {
                "$ref": "l1.json#/$defs/Expr",
                "description": "A value."
              },
              "description": "Values, in order, paired positionally with `keys`."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            },
            "entries": {
              "type": "array",
              "items": {
                "$ref": "l1.json#/$defs/Expr",
                "description": "A map element: `logic.MapEntry`-shaped `logic.MapLit` with one key/value, a spread, or a collection-if/for whose element is one of those."
              },
              "description": "Present instead of `keys`/`values` when the literal has a spread or a collection-if/for; each entry is a one-entry `logic.MapLit`, a `logic.Spread`, an `logic.IfElement` or a `logic.ForElement`."
            }
          }
        }
      ]
    },
    "New": {
      "description": "A constructor invocation.",
      "x-uir-kind": "logic.New",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "typeName",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.New",
              "description": "Discriminant."
            },
            "typeName": {
              "type": "string",
              "description": "The class constructed."
            },
            "args": {
              "type": "array",
              "items": {
                "$ref": "l1.json#/$defs/Expr",
                "description": "An argument."
              },
              "description": "Positional arguments, in order."
            },
            "namedArgs": {
              "type": "object",
              "additionalProperties": {
                "$ref": "l1.json#/$defs/Expr",
                "description": "A named argument."
              },
              "description": "Named arguments."
            },
            "namedArgOrder": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "The labels of `namedArgs`, in this call's own real source (left-to-right) argument order (ADR-0037) — never sorted, never reordered to a constructor's own declaration order. Present only alongside `namedArgs`. Exists because `namedArgs` itself is an ordinary map, canonicalized to sorted key order by the builder (`RawMap`'s own documented contract) like every other named-argument-carrying node in the schema — a property that does not matter for a `namedArgs` consumer that only reads *values*, but does matter for a bounded structural construction (ADR-0036) lowering to an object literal, where Dart's own left-to-right argument evaluation order must survive into the emitted property order even when it differs from the constructed class's own field-declaration order. Scoped to `logic.New` alone — no other `namedArgs`-carrying node kind gained this field, since none needed it for this milestone."
            },
            "isConst": {
              "type": "boolean",
              "description": "Whether the construction is const — the input to const folding (pass N6)."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            },
            "constructorName": {
              "type": "string",
              "description": "The named constructor, if any."
            },
            "presentedContent": {
              "$ref": "l2.json#/$defs/UiNode",
              "description": "This construction's own widget content, embedded (ADR-0030).\n\n**Present only on a `SnackBar`'s own `logic.New` node, and only when extraction recognized `ScaffoldMessenger.of(context).showSnackBar(SnackBar(...))` as a direct inline construction** — never inferred from the class name alone; recognition is by the resolved receiver/constructed type (ADR-0030 §6). The real, catalog-extracted widget subtree for the `content:` argument, embedded here the same way `ui.Cond.then`/`ui.List.template`/`app.RouteTransition.inline` already embed a subtree rather than referencing one (M9-D) — an id is not available to reference by at the point extraction runs. Replaces, rather than duplicates, the argument's own entry under `namedArgs`: a recognized `SnackBar`'s own `namedArgs` carries no `content` key at all, so the same expression is never extracted twice under two different anchors.\n\nAbsent on every other `logic.New` node in the document, including a `SnackBar(...)` reached any other way (stored in a variable, never passed to `showSnackBar`, or passed by reference rather than as a direct literal — ADR-0030 §12), in which case `content` stays an ordinary `namedArgs` entry."
            }
          }
        }
      ]
    },
    "Cast": {
      "description": "An `as` cast.",
      "x-uir-kind": "logic.Cast",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "operand",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Cast",
              "description": "Discriminant."
            },
            "operand": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The value cast."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "The target type."
            }
          }
        }
      ]
    },
    "StringInterp": {
      "description": "An interpolated string. Each interpolation is a reactive read.",
      "x-uir-kind": "logic.StringInterp",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.StringInterp",
              "description": "Discriminant."
            },
            "parts": {
              "type": "array",
              "items": {
                "$ref": "l1.json#/$defs/Expr",
                "description": "A part: a literal fragment or an interpolated expression."
              },
              "description": "Parts, in order."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            }
          }
        }
      ]
    },
    "NullCheck": {
      "description": "A null-aware operation, e.g. `a ?? b` or `a?.b`.",
      "x-uir-kind": "logic.NullCheck",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "operand",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.NullCheck",
              "description": "Discriminant."
            },
            "operand": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The operand."
            },
            "fallback": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The fallback, for `??`."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            }
          }
        }
      ]
    },
    "Intrinsic": {
      "description": "A value the host framework provides, which the program does not declare (ADR-0026).\n\nUIR's other two ways to spell a reference both need something the program contains: `logic.Ref{target}` a declaration, `logic.Ref{name}` a lexically enclosing parameter. A framework intrinsic like Flutter's `State.mounted`/`BuildContext.mounted` is neither — recognized by the analyzer from the resolved element (never from the spelling `mounted`, which an application's own field could share), and represented here as the target-neutral fact it answers rather than the framework API that answers it.\n\nExpression-level, not a statement-level guard: `docs/m6/GAP-mounted.md`'s corpus measurement found `mounted` used as one boolean operand among several (`if (result == null || !mounted) return;`) in 58 of 348 real occurrences, which only an ordinary composable expression node can represent.",
      "x-uir-kind": "logic.Intrinsic",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "intrinsic",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Intrinsic",
              "description": "Discriminant."
            },
            "intrinsic": {
              "$ref": "#/$defs/IntrinsicKind",
              "description": "Which framework-provided fact this reads."
            },
            "operand": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The value the intrinsic is about, for a member that takes one (`context.mounted`'s context value). Absent for a nullary member (`component.mounted`)."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            }
          }
        }
      ]
    },
    "OpaqueExpr": {
      "description": "An expression the extractor cannot model.\n\nNever dropped, never guessed (INV-4): the raw source, its resolved type, and the reason are preserved so the override system and the AI gap analyzer both have something real to work with.",
      "x-uir-kind": "logic.OpaqueExpr",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "dartSource",
            "reason",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.OpaqueExpr",
              "description": "Discriminant."
            },
            "dartSource": {
              "type": "string",
              "description": "The original Dart source."
            },
            "reason": {
              "type": "string",
              "description": "Why it could not be modeled."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type, which the analyzer still knows."
            }
          }
        }
      ]
    },
    "Block": {
      "description": "A brace-delimited block.",
      "x-uir-kind": "logic.Block",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Block",
              "description": "Discriminant."
            },
            "statements": {
              "type": "array",
              "items": {
                "$ref": "l1.json#/$defs/Stmt",
                "description": "A statement."
              },
              "description": "Statements, in order. Order is semantic."
            }
          }
        }
      ]
    },
    "If": {
      "description": "An if statement.",
      "x-uir-kind": "logic.If",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "test",
            "then"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.If",
              "description": "Discriminant."
            },
            "test": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The condition."
            },
            "then": {
              "$ref": "l1.json#/$defs/Stmt",
              "description": "The then branch."
            },
            "otherwise": {
              "$ref": "l1.json#/$defs/Stmt",
              "description": "The else branch, if any."
            }
          }
        }
      ]
    },
    "For": {
      "description": "A for or for-in loop.",
      "x-uir-kind": "logic.For",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "body"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.For",
              "description": "Discriminant."
            },
            "init": {
              "$ref": "l1.json#/$defs/Stmt",
              "description": "Initializer, for a C-style loop."
            },
            "test": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "Loop condition."
            },
            "update": {
              "type": "array",
              "items": {
                "$ref": "l1.json#/$defs/Expr",
                "description": "An update expression."
              },
              "description": "Update expressions, in order."
            },
            "iterable": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The iterable, for a for-in loop."
            },
            "loopVariable": {
              "type": "string",
              "description": "The loop variable's own name, for a for-in loop, by name only. `loopDecl` (ADR-28, amended M9-A) is the same binding's own declaration-tier identity — this field is retained unchanged, for description only."
            },
            "loopDecl": {
              "$ref": "#/$defs/VarDecl",
              "description": "The for-in loop variable's own declaration (ADR-28, amended M9-A) — a real, declaration-tier `logic.VarDecl`, with no `initializer` (the runtime binds it, once per iteration, not an expression). A `logic.Ref` inside the loop body targets this node's own id, the same way any other local variable's read already does. Absent exactly when `loopVariable` is (a C-style loop, not a for-in loop)."
            },
            "body": {
              "$ref": "l1.json#/$defs/Stmt",
              "description": "The loop body."
            }
          }
        }
      ]
    },
    "While": {
      "description": "A while or do-while loop.",
      "x-uir-kind": "logic.While",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "test",
            "body"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.While",
              "description": "Discriminant."
            },
            "test": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The condition."
            },
            "body": {
              "$ref": "l1.json#/$defs/Stmt",
              "description": "The body."
            },
            "isDoWhile": {
              "type": "boolean",
              "description": "Whether the test runs after the body."
            }
          }
        }
      ]
    },
    "Switch": {
      "description": "A switch statement.",
      "x-uir-kind": "logic.Switch",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "subject"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Switch",
              "description": "Discriminant."
            },
            "subject": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The value switched on."
            },
            "cases": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SwitchCase",
                "description": "A case."
              },
              "description": "Cases, in order."
            }
          }
        }
      ]
    },
    "Return": {
      "description": "A return statement.",
      "x-uir-kind": "logic.Return",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Return",
              "description": "Discriminant."
            },
            "value": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The returned value, if any."
            }
          }
        }
      ]
    },
    "Throw": {
      "description": "A throw statement.",
      "x-uir-kind": "logic.Throw",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "value"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Throw",
              "description": "Discriminant."
            },
            "value": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The thrown value."
            }
          }
        }
      ]
    },
    "TryCatch": {
      "description": "A try statement.",
      "x-uir-kind": "logic.TryCatch",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "body"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.TryCatch",
              "description": "Discriminant."
            },
            "body": {
              "$ref": "#/$defs/Block",
              "description": "The protected block."
            },
            "catches": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/CatchClause",
                "description": "A catch clause."
              },
              "description": "Catch clauses, in order."
            },
            "finallyBlock": {
              "$ref": "#/$defs/Block",
              "description": "The finally block, if any."
            }
          }
        }
      ]
    },
    "VarDecl": {
      "description": "A local variable declaration.",
      "x-uir-kind": "logic.VarDecl",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "name",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.VarDecl",
              "description": "Discriminant."
            },
            "name": {
              "type": "string",
              "description": "The variable name."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            },
            "initializer": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The initializer, if any."
            },
            "isFinal": {
              "type": "boolean",
              "description": "Whether the variable is final."
            }
          }
        }
      ]
    },
    "ExprStmt": {
      "description": "An expression evaluated for its effect.",
      "x-uir-kind": "logic.ExprStmt",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "expr"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.ExprStmt",
              "description": "Discriminant."
            },
            "expr": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The expression."
            }
          }
        }
      ]
    },
    "Break": {
      "description": "A break statement.",
      "x-uir-kind": "logic.Break",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Break",
              "description": "Discriminant."
            },
            "label": {
              "type": "string",
              "description": "The target label, if any."
            }
          }
        }
      ]
    },
    "Continue": {
      "description": "A continue statement.",
      "x-uir-kind": "logic.Continue",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Continue",
              "description": "Discriminant."
            },
            "label": {
              "type": "string",
              "description": "The target label, if any."
            }
          }
        }
      ]
    },
    "OpaqueStmt": {
      "description": "A statement the extractor cannot model. Preserved rather than dropped (INV-4).",
      "x-uir-kind": "logic.OpaqueStmt",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "dartSource",
            "reason"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.OpaqueStmt",
              "description": "Discriminant."
            },
            "dartSource": {
              "type": "string",
              "description": "The original Dart source."
            },
            "reason": {
              "type": "string",
              "description": "Why it could not be modeled."
            }
          }
        }
      ]
    },
    "Navigate": {
      "description": "Performing a navigation — a push, a pop, or the opening of a route overlay (ADR-0025 D2).\n\n`app.RouteTransition` is a *declarative edge in the navigation graph*; it is the input to N11 (ADR-11) and says where an application **can** go. It does not say where it **does** go, and nothing else did either: a `Navigator.pushNamed` survived extraction as an ordinary call to an unresolvable name, in violation of INV-22, and the generator had a route table, a live router and a call it could not connect to either.\n\nThis is that connection, and it is a **statement** rather than a field on the edge for one measured reason: a pop has no edge. Spec v2.4 §A17.3 rules that a pop returns along an edge that already exists rather than creating one, and the M6-D corpus measured pops as the most frequent navigation verb in real Flutter — 143 uses against 83 pushes. A `site` field on `app.RouteTransition` could not have expressed one.\n\n**Scope.** The result of a navigation is not modelled: this node is evaluated for effect. `final ok = await showDialog<bool>(...)` — 19 measured sites — is still refused, because a statement carries no value. See `docs/m7/m7a-navigation-implementation.md`.",
      "x-uir-kind": "logic.Navigate",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "action"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Navigate",
              "description": "Discriminant."
            },
            "action": {
              "$ref": "#/$defs/NavigateAction",
              "description": "What the navigation does."
            },
            "transition": {
              "$ref": "shared.json#/$defs/NodeId",
              "description": "The `app.RouteTransition` this performs, for a departure.\n\n**Absent for a return** (`pop`, `popUntil`, `maybePop`): §A17.3 rules that a pop is not a transition, so there is no edge to name. A reader must not treat its absence as an incomplete document — `action` says which case it is."
            },
            "dismisses": {
              "$ref": "shared.json#/$defs/NodeId",
              "description": "The `app.RouteTransition` (with an `inline` destination) this `pop` dismisses (M9-E, ADR-0025 amendment `0025-amendment-dialog-dismissal-scope.md`).\n\n**Present only for `action: 'pop'`, and only when extraction proved the pop is lexically inside that transition's own presentation** — `Navigator.pop(dialogContext)`, or any other `Navigator.pop(...)`/`maybePop(...)`, reached anywhere inside a `showDialog(builder: ...)`'s own extracted widget subtree (e.g. `AlertDialog.actions`). Never present for an ordinary page-level pop, and never inferred from *which* `BuildContext` expression was written — only from where extraction found the call. `action` alone does not distinguish a dialog dismissal from a route pop; a reader checks whether this field is present.\n\n`popUntil` is unaffected and never carries this field — its own predicate remains unmodelled (§A17.3)."
            }
          }
        }
      ]
    },
    "ClassDecl": {
      "description": "A class declaration.",
      "x-uir-kind": "logic.ClassDecl",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "name"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.ClassDecl",
              "description": "Discriminant."
            },
            "name": {
              "type": "string",
              "description": "Class name."
            },
            "superclass": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "The superclass, if any."
            },
            "fields": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FieldDecl",
                "description": "A field."
              },
              "description": "Fields, in declaration order."
            },
            "methods": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FunctionDecl",
                "description": "A method."
              },
              "description": "Methods, in declaration order."
            },
            "constructibleConstructors": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ConstructibleConstructor",
                "description": "One safely structural constructor."
              },
              "description": "One entry per this class's own constructor that is safely equivalent to a plain, immutable record's own construction (ADR-0036, generalized by ADR-0037 from a single class-global mapping to a constructor-keyed list): non-const, non-factory, non-redirecting, an empty body and an empty initializer list, and field-formal parameters — uniformly required-positional or uniformly required-named, never mixed — that cover every one of the class's own instance fields exactly once. The whole-class prerequisite is unchanged from ADR-0036: every instance field public/final/non-static/non-late, and the class itself public, non-generic, with no superclass/`implements`/`with`. A constructor failing its own eligibility (a body, a factory keyword, an incomplete field-formal set, a mix of positional and named field-formals, and so on) is simply absent from this array — it neither disqualifies a sibling constructor nor the class's own other, eligible constructors (ADR-0037 §9). Present as an empty array when the class satisfies the whole-class prerequisite but has no individually eligible constructor; present as a single implicit-unnamed-positional entry with an empty `fields` array for a fieldless class with no explicit constructor; absent entirely when the whole-class prerequisite itself fails. Constructor identity is `(this ClassDecl, this entry's own name)` — already unique, since Dart forbids two constructors sharing one name on one class, and two different classes never share a `ClassDecl` id — so no separate constructor-identity node or symbol scheme was introduced. Derived exclusively from `FieldFormalParameterElement.field` — never from parameter-name or field-name text equality. Declaration provenance only — never a claim that any constructor is invoked at runtime; a generator resolving a construction against an entry here emits a plain object literal, never a call to the constructor this entry describes."
            },
            "library": {
              "type": "string",
              "description": "The library file this class belongs to — its own path, or for a declaration in a `part of` file the path of the library that includes it. Absent for a class of a synthesized declaration. A generated module is per library (M12, ADR-0055)."
            },
            "isAbstract": {
              "type": "boolean",
              "description": "Whether the class is `abstract` (or `sealed`, which is abstract). Present only when true."
            },
            "isMixin": {
              "type": "boolean",
              "description": "A `mixin` declaration (M12, ADR-0059): its members are added to every class that applies it with `with`. Not itself constructed."
            },
            "typeParameters": {
              "type": "array",
              "items": {
                "type": "string",
                "description": "A type parameter name."
              },
              "description": "The class's type parameters, in order (`class Box<T>`). Absent for a non-generic class."
            },
            "interfaces": {
              "type": "array",
              "items": {
                "$ref": "shared.json#/$defs/TypeRef",
                "description": "An `implements` type."
              },
              "description": "The types this class `implements`, in order."
            },
            "mixins": {
              "type": "array",
              "items": {
                "$ref": "shared.json#/$defs/TypeRef",
                "description": "A `with` type."
              },
              "description": "The mixins this class applies (`with`), in order."
            },
            "constructors": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ConstructorDecl",
                "description": "A constructor."
              },
              "description": "Every constructor the class declares, in declaration order (M12, ADR-0055). Unlike `constructibleConstructors` — which lists only the few a plain record could stand in for — this is the whole set, with bodies and initializers, for a class the generator emits as a real class."
            }
          }
        }
      ]
    },
    "EnumDecl": {
      "description": "An enum declaration.",
      "x-uir-kind": "logic.EnumDecl",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "name"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.EnumDecl",
              "description": "Discriminant."
            },
            "name": {
              "type": "string",
              "description": "Enum name."
            },
            "values": {
              "type": "array",
              "items": {
                "type": "string",
                "description": "A value name."
              },
              "description": "Values, in declaration order."
            },
            "library": {
              "type": "string",
              "description": "For an *enhanced* enum (one with fields, methods or constants with arguments), the library file it belongs to — present only then. An enhanced enum is emitted as a class with one static instance per constant; a plain enum is its value names (M12, ADR-0056)."
            },
            "fields": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FieldDecl",
                "description": "A field."
              },
              "description": "An enhanced enum's instance fields, in declaration order."
            },
            "methods": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FunctionDecl",
                "description": "A method."
              },
              "description": "An enhanced enum's methods, getters and operators."
            },
            "constructors": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ConstructorDecl",
                "description": "A constructor."
              },
              "description": "An enhanced enum's constructors (always `const`)."
            },
            "constants": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/EnumConstant",
                "description": "A constant."
              },
              "description": "An enhanced enum's constants, in declaration order, with the arguments each is constructed from."
            }
          }
        }
      ]
    },
    "TypeAliasDecl": {
      "description": "A typedef.",
      "x-uir-kind": "logic.TypeAliasDecl",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "name",
            "aliased"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.TypeAliasDecl",
              "description": "Discriminant."
            },
            "name": {
              "type": "string",
              "description": "Alias name."
            },
            "aliased": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "The aliased type."
            }
          }
        }
      ]
    },
    "FunctionDecl": {
      "description": "A function or method declaration.",
      "x-uir-kind": "logic.FunctionDecl",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "name",
            "returnType"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.FunctionDecl",
              "description": "Discriminant."
            },
            "name": {
              "type": "string",
              "description": "Function name."
            },
            "params": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ParamDecl",
                "description": "A parameter."
              },
              "description": "Parameters, in order."
            },
            "returnType": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved return type."
            },
            "body": {
              "type": "array",
              "items": {
                "$ref": "l1.json#/$defs/Stmt",
                "description": "A statement."
              },
              "description": "Body, in order."
            },
            "isAsync": {
              "type": "boolean",
              "description": "Whether the function is async."
            },
            "isStatic": {
              "type": "boolean",
              "description": "Whether the function is static."
            },
            "isGetter": {
              "type": "boolean",
              "description": "Whether this declaration is a class's own explicit instance getter (ADR-0038), as opposed to an ordinary method or a setter. Present only on a `logic.FunctionDecl` embedded in `ClassDecl.methods` — a top-level `logic.FunctionDecl` (ADR-29) is never marked this way, since Dart's own top-level `get` declarations extract identically to an ordinary top-level function (`declaration_extractor.dart`'s own `_function`), the same way this schema has always treated one; there is no `isGetter` distinction to make at that level. A class's own setter continues to be represented as an ordinary, unflagged `logic.FunctionDecl` (its own symbol mangled with a trailing `=`, unchanged since before M9-Q) — `isGetter` absent (never `false`) for it, exactly as for an ordinary method; M9-Q's own bounded execution model reads `isGetter === true` as the one positive signal it needs, never infers a setter from its absence."
            },
            "isOperator": {
              "type": "boolean",
              "description": "Whether this is an operator declaration (`operator ==`, `operator []`); its `name` is the operator token. Present only when true (M12)."
            },
            "isSetter": {
              "type": "boolean",
              "description": "Whether this is a setter (`set value(int v)`), as opposed to a getter of the same name. Present only when true (M12)."
            },
            "isAbstract": {
              "type": "boolean",
              "description": "Whether the member has no body (`abstract` or an interface's). Present only when true (M12)."
            },
            "typeParameters": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "The names of a generic function's or method's own type parameters (`T identity<T>(T v)`)."
            },
            "extensionOn": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Set for an extension member (ADR-0068): the type the extension extends. The member takes it as its receiver (`this`); a call site names the member with `extensionTarget`."
            }
          }
        }
      ]
    },
    "FieldDecl": {
      "description": "A field declaration.",
      "x-uir-kind": "logic.FieldDecl",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "name",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.FieldDecl",
              "description": "Discriminant."
            },
            "name": {
              "type": "string",
              "description": "Field name."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            },
            "initializer": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The initializer, if any."
            },
            "isFinal": {
              "type": "boolean",
              "description": "Whether the field is final."
            },
            "isStatic": {
              "type": "boolean",
              "description": "Whether the field is static."
            },
            "isLate": {
              "type": "boolean",
              "description": "Whether the field was declared with the `late` modifier (ADR-0035). `late` carries its own runtime initialization/error semantics a bounded, side-effect-free field-shape read must not misrepresent — this is the one field-eligibility fact `isFinal`/`isStatic` cannot express and the generator has no other schema source for, so it is recorded explicitly rather than inferred."
            }
          }
        }
      ]
    },
    "OpaqueDecl": {
      "description": "A declaration the extractor cannot model — a mixin, an extension. Preserved rather than dropped (INV-4).\n\nAdded in v2.2 (§A11). The `Decl` union previously had no opaque variant, unlike `Expr` and `Stmt`, so an unmodellable declaration had nowhere to go and would have had to be silently discarded. compass_app declares 11 mixins; wonderous declares 5 extensions.",
      "x-uir-kind": "logic.OpaqueDecl",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "dartSource",
            "reason"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.OpaqueDecl",
              "description": "Discriminant."
            },
            "dartSource": {
              "type": "string",
              "description": "The original Dart source."
            },
            "reason": {
              "type": "string",
              "description": "Why it could not be modeled."
            }
          }
        }
      ]
    },
    "Expr": {
      "description": "Any expression.",
      "x-uir-union": true,
      "oneOf": [
        {
          "$ref": "#/$defs/Lit"
        },
        {
          "$ref": "#/$defs/Ref"
        },
        {
          "$ref": "#/$defs/Call"
        },
        {
          "$ref": "#/$defs/MethodCall"
        },
        {
          "$ref": "#/$defs/PropertyAccess"
        },
        {
          "$ref": "#/$defs/Binary"
        },
        {
          "$ref": "#/$defs/Unary"
        },
        {
          "$ref": "#/$defs/Assign"
        },
        {
          "$ref": "#/$defs/Conditional"
        },
        {
          "$ref": "#/$defs/Lambda"
        },
        {
          "$ref": "#/$defs/Await"
        },
        {
          "$ref": "#/$defs/ListLit"
        },
        {
          "$ref": "#/$defs/MapLit"
        },
        {
          "$ref": "#/$defs/New"
        },
        {
          "$ref": "#/$defs/Cast"
        },
        {
          "$ref": "#/$defs/TypeCheck"
        },
        {
          "$ref": "#/$defs/StringInterp"
        },
        {
          "$ref": "#/$defs/NullCheck"
        },
        {
          "$ref": "#/$defs/Intrinsic"
        },
        {
          "$ref": "#/$defs/ThrowExpr"
        },
        {
          "$ref": "#/$defs/Rethrow"
        },
        {
          "$ref": "#/$defs/Sequence"
        },
        {
          "$ref": "#/$defs/Let"
        },
        {
          "$ref": "#/$defs/Spread"
        },
        {
          "$ref": "#/$defs/IfElement"
        },
        {
          "$ref": "#/$defs/ForElement"
        },
        {
          "$ref": "#/$defs/WidgetExpr"
        },
        {
          "$ref": "#/$defs/SwitchExpr"
        },
        {
          "$ref": "#/$defs/PatternMatch"
        },
        {
          "$ref": "#/$defs/RecordLit"
        },
        {
          "$ref": "#/$defs/OpaqueExpr"
        }
      ]
    },
    "Stmt": {
      "description": "Any statement.",
      "x-uir-union": true,
      "oneOf": [
        {
          "$ref": "#/$defs/Block"
        },
        {
          "$ref": "#/$defs/If"
        },
        {
          "$ref": "#/$defs/For"
        },
        {
          "$ref": "#/$defs/While"
        },
        {
          "$ref": "#/$defs/Switch"
        },
        {
          "$ref": "#/$defs/Return"
        },
        {
          "$ref": "#/$defs/Throw"
        },
        {
          "$ref": "#/$defs/TryCatch"
        },
        {
          "$ref": "#/$defs/VarDecl"
        },
        {
          "$ref": "#/$defs/ExprStmt"
        },
        {
          "$ref": "#/$defs/Navigate"
        },
        {
          "$ref": "#/$defs/Break"
        },
        {
          "$ref": "#/$defs/Continue"
        },
        {
          "$ref": "#/$defs/PatternDecl"
        },
        {
          "$ref": "#/$defs/OpaqueStmt"
        }
      ]
    },
    "Decl": {
      "description": "Any declaration.",
      "x-uir-union": true,
      "oneOf": [
        {
          "$ref": "#/$defs/ClassDecl"
        },
        {
          "$ref": "#/$defs/EnumDecl"
        },
        {
          "$ref": "#/$defs/TypeAliasDecl"
        },
        {
          "$ref": "#/$defs/FunctionDecl"
        },
        {
          "$ref": "#/$defs/FieldDecl"
        },
        {
          "$ref": "#/$defs/OpaqueDecl"
        }
      ]
    },
    "ConstructorInit": {
      "description": "One `field = value` entry of a constructor's initializer list.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "field",
        "value"
      ],
      "properties": {
        "field": {
          "type": "string",
          "description": "The field initialized."
        },
        "value": {
          "$ref": "#/$defs/Expr",
          "description": "The value."
        }
      }
    },
    "ConstructorCall": {
      "description": "A call to another constructor: `super(...)`, `super.named(...)`, `this(...)` or `this.named(...)`.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "constructorName": {
          "type": "string",
          "description": "The named constructor called. Absent for the unnamed one."
        },
        "args": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Expr",
            "description": "A positional argument."
          },
          "description": "Positional arguments, in order."
        },
        "namedArgs": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/Expr"
          },
          "description": "Named arguments, by name."
        }
      }
    },
    "ConstructorDecl": {
      "description": "A constructor of a class (M12, ADR-0055).",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "The constructor's name. Absent for the unnamed constructor."
        },
        "params": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ParamDecl",
            "description": "A parameter."
          },
          "description": "Parameters, in order. An initializing formal (`this.x`) carries `initializesField`."
        },
        "isConst": {
          "type": "boolean",
          "description": "Whether declared `const`. Present only when true."
        },
        "isFactory": {
          "type": "boolean",
          "description": "Whether declared `factory`. Present only when true."
        },
        "initializers": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ConstructorInit",
            "description": "An initializer."
          },
          "description": "The initializer list's `field = value` entries, in order. `assert` entries are not carried."
        },
        "superCall": {
          "$ref": "#/$defs/ConstructorCall",
          "description": "The `super(...)` call, if the initializer list has one."
        },
        "redirectsTo": {
          "$ref": "#/$defs/ConstructorCall",
          "description": "For a redirecting generative constructor (`: this(...)`), the sibling constructor it delegates to."
        },
        "redirectedFactory": {
          "$ref": "#/$defs/RedirectedFactory",
          "description": "For a redirecting factory (`factory A() = _A;`), the constructor it is defined as."
        },
        "body": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Stmt",
            "description": "A statement."
          },
          "description": "The body, in order. Absent for an empty or absent body."
        }
      }
    },
    "RedirectedFactory": {
      "description": "The constructor a redirecting factory is defined as.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "type"
      ],
      "properties": {
        "type": {
          "$ref": "shared.json#/$defs/TypeRef",
          "description": "The class the factory redirects to (`= _Impl`)."
        },
        "constructorName": {
          "type": "string",
          "description": "Its constructor, if named."
        }
      }
    },
    "TypeCheck": {
      "description": "An `is` / `is!` type test: `value is Type`.",
      "x-uir-kind": "logic.TypeCheck",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "operand",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.TypeCheck",
              "description": "Discriminant."
            },
            "operand": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The value tested."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "The type tested against."
            },
            "negated": {
              "type": "boolean",
              "description": "Whether this is `is!`. Present only when true."
            }
          }
        }
      ]
    },
    "EnumConstant": {
      "description": "One constant of an enhanced enum: `words(floor: 0.45)`.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name"
      ],
      "properties": {
        "name": {
          "type": "string",
          "description": "The constant's name."
        },
        "constructorName": {
          "type": "string",
          "description": "The named constructor it uses (`low.named(1)`). Absent for the unnamed one."
        },
        "args": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Expr",
            "description": "A positional argument."
          },
          "description": "Positional arguments, in order."
        },
        "namedArgs": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/Expr"
          },
          "description": "Named arguments, by name."
        }
      }
    },
    "ThrowExpr": {
      "description": "A `throw` in expression position: `x ?? throw Foo()`, `=> throw Foo()`, a switch arm. Its type is `Never`.",
      "x-uir-kind": "logic.ThrowExpr",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "value",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.ThrowExpr",
              "description": "Discriminant."
            },
            "value": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The thrown value."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            }
          }
        }
      ]
    },
    "Rethrow": {
      "description": "`rethrow`: throws the exception the enclosing catch clause caught, with its original stack.",
      "x-uir-kind": "logic.Rethrow",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Rethrow",
              "description": "Discriminant."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            }
          }
        }
      ]
    },
    "Sequence": {
      "description": "Expressions evaluated in order; the value is the last one's (`(a, b, c)` in JavaScript).",
      "x-uir-kind": "logic.Sequence",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "exprs",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Sequence",
              "description": "Discriminant."
            },
            "exprs": {
              "type": "array",
              "items": {
                "$ref": "l1.json#/$defs/Expr",
                "description": "An expression."
              },
              "description": "In order; at least one."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            }
          }
        }
      ]
    },
    "Let": {
      "description": "Binds a value once and evaluates `body` with it in scope: a null-aware access on a receiver that is not a plain variable (`(json['a'] as num?)?.toDouble()`), and a cascade (`Paint()..color = c`). A `logic.Ref` in `body` targets `binding`'s own id.",
      "x-uir-kind": "logic.Let",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "binding",
            "body",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Let",
              "description": "Discriminant."
            },
            "binding": {
              "$ref": "#/$defs/VarDecl",
              "description": "The bound variable; its `initializer` is the value."
            },
            "body": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The expression evaluated with the binding in scope."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            }
          }
        }
      ]
    },
    "Spread": {
      "description": "`...xs` / `...?xs` as an element of a collection literal.",
      "x-uir-kind": "logic.Spread",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "value",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Spread",
              "description": "Discriminant."
            },
            "value": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The spread collection."
            },
            "nullAware": {
              "type": "boolean",
              "description": "`...?`: a null collection spreads nothing."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            }
          }
        }
      ]
    },
    "IfElement": {
      "description": "`if (test) a else b` as an element of a collection literal. `then`/`otherwise` are elements: an expression, a spread, or another collection-if/for.",
      "x-uir-kind": "logic.IfElement",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "test",
            "then",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.IfElement",
              "description": "Discriminant."
            },
            "test": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The condition."
            },
            "then": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The element when true."
            },
            "otherwise": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The element when false, if any."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            }
          }
        }
      ]
    },
    "ForElement": {
      "description": "`for (final x in xs) element` or `for (var i = 0; i < n; i++) element` as an element of a collection literal.",
      "x-uir-kind": "logic.ForElement",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "body",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.ForElement",
              "description": "Discriminant."
            },
            "loopDecl": {
              "$ref": "#/$defs/VarDecl",
              "description": "For-in: the loop variable's declaration; a `logic.Ref` in `body` resolves to it by name."
            },
            "iterable": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "For-in: the iterable."
            },
            "body": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The element produced per iteration: an expression, a spread, or a nested collection-if/for."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            },
            "init": {
              "$ref": "#/$defs/VarDecl",
              "description": "C-style `for (var i = 0; …)`: the declaration; each iteration sees its own copy, as in Dart."
            },
            "test": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "C-style: the condition."
            },
            "update": {
              "type": "array",
              "items": {
                "$ref": "l1.json#/$defs/Expr",
                "description": "An update expression."
              },
              "description": "C-style: the update expressions, in order."
            }
          }
        }
      ]
    },
    "WidgetExpr": {
      "description": "A widget as a value inside logic code (M12, ADR-0062): `children.add(Padding(child: w))` in a statement-bodied `build`. `tree` is the same `ui.*` tree a render position holds; the generator emits it where the expression is.",
      "x-uir-kind": "logic.WidgetExpr",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "tree",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.WidgetExpr",
              "description": "Discriminant."
            },
            "tree": {
              "$ref": "l2.json#/$defs/UiNode",
              "description": "The widget's tree."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type (a `Widget`)."
            }
          }
        }
      ]
    },
    "Pattern": {
      "description": "A Dart 3 pattern (ADR-0065): what a `switch` case or a switch-expression arm matches. `variant` says which; the other fields are the ones that variant uses.",
      "x-uir-kind": "logic.Pattern",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "variant"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.Pattern",
              "description": "Discriminant."
            },
            "variant": {
              "type": "string",
              "enum": [
                "const",
                "wildcard",
                "bind",
                "object",
                "or",
                "and",
                "relational",
                "nullCheck",
                "nullAssert",
                "cast",
                "list",
                "map",
                "rest"
              ],
              "description": "`const`: `value` (a literal, an enum constant, a `const`); `wildcard`: `_`; `bind`: `var x` / `T x` — `decl` and an optional `matchType`; `object`: `Type(field: pattern, …)` — `matchType` and `fields`; `or`/`and`: `patterns`; `relational`: `operator` `value`; `nullCheck`: `x?`; `nullAssert`: `x!`; `cast`: `x as T` — `pattern` and `matchType`. `list`: `patterns` (elements, at most one `rest`); `rest`: `...` or `...var r` with an optional `pattern`; `map`: `entries`; a record pattern is an `object` pattern with no `matchType` whose `fields` are `$1`… and names."
            },
            "value": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "A `const`/`relational` pattern's value."
            },
            "operator": {
              "type": "string",
              "description": "A `relational` pattern's operator."
            },
            "decl": {
              "$ref": "#/$defs/VarDecl",
              "description": "A `bind` pattern's variable (no initializer); reads of it resolve by name."
            },
            "matchType": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "The type a `bind`/`object`/`cast` pattern tests."
            },
            "fields": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PatternField"
              },
              "description": "An `object` pattern's field patterns."
            },
            "patterns": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Pattern"
              },
              "description": "`or`/`and` operands."
            },
            "pattern": {
              "$ref": "#/$defs/Pattern",
              "description": "The inner pattern of a `nullCheck`/`nullAssert`/`cast`."
            },
            "entries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PatternMapEntry"
              },
              "description": "A `map` pattern's entries: the key must be present and its value match."
            }
          }
        }
      ]
    },
    "SwitchExprCase": {
      "type": "object",
      "description": "One arm of a switch expression: `pattern when guard => value`.",
      "additionalProperties": false,
      "required": [
        "pattern",
        "value"
      ],
      "properties": {
        "pattern": {
          "$ref": "#/$defs/Pattern",
          "description": "What the arm matches."
        },
        "guard": {
          "$ref": "l1.json#/$defs/Expr",
          "description": "The `when` clause."
        },
        "value": {
          "$ref": "l1.json#/$defs/Expr",
          "description": "The arm's value."
        }
      }
    },
    "SwitchExpr": {
      "description": "A `switch` expression (ADR-0065). Exhaustive by Dart's rules: when no arm matches it throws.",
      "x-uir-kind": "logic.SwitchExpr",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "subject",
            "cases",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.SwitchExpr",
              "description": "Discriminant."
            },
            "subject": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The value matched."
            },
            "cases": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SwitchExprCase"
              },
              "description": "Arms, in order."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved type."
            }
          }
        }
      ]
    },
    "PatternField": {
      "type": "object",
      "description": "One field of an `object` pattern.",
      "additionalProperties": false,
      "required": [
        "name",
        "pattern"
      ],
      "properties": {
        "name": {
          "type": "string",
          "description": "The getter read."
        },
        "pattern": {
          "$ref": "#/$defs/Pattern",
          "description": "What its value must match."
        }
      }
    },
    "RecordLit": {
      "description": "A record literal, `(1, 'a')` / `(id: 1, name: 'a')` (ADR-0069). Positional fields are `$1`, `$2`, … and named ones are their names.",
      "x-uir-kind": "logic.RecordLit",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.RecordLit",
              "description": "Discriminant."
            },
            "positional": {
              "type": "array",
              "items": {
                "$ref": "l1.json#/$defs/Expr",
                "description": "A positional field."
              },
              "description": "Positional fields, in order."
            },
            "named": {
              "type": "object",
              "additionalProperties": {
                "$ref": "l1.json#/$defs/Expr",
                "description": "A named field."
              },
              "description": "Named fields."
            },
            "namedOrder": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "The labels of `named` in source order."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "Resolved record type."
            }
          }
        }
      ]
    },
    "PatternMatch": {
      "description": "`value case pattern when guard` as a condition (ADR-0069): true when the pattern (and guard) match; the variables it binds are in scope in the branch the condition selects.",
      "x-uir-kind": "logic.PatternMatch",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "subject",
            "pattern",
            "type"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.PatternMatch",
              "description": "Discriminant."
            },
            "subject": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The value matched."
            },
            "pattern": {
              "$ref": "#/$defs/Pattern",
              "description": "The pattern."
            },
            "guard": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The `when` clause."
            },
            "type": {
              "$ref": "shared.json#/$defs/TypeRef",
              "description": "`bool`."
            }
          }
        }
      ]
    },
    "PatternDecl": {
      "description": "A pattern variable declaration, `final (a, b) = value;` (ADR-0069): the pattern's variables are declared for the rest of the block.",
      "x-uir-kind": "logic.PatternDecl",
      "allOf": [
        {
          "$ref": "shared.json#/$defs/UirNodeBase"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "pattern",
            "value"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "logic.PatternDecl",
              "description": "Discriminant."
            },
            "pattern": {
              "$ref": "#/$defs/Pattern",
              "description": "The pattern."
            },
            "value": {
              "$ref": "l1.json#/$defs/Expr",
              "description": "The value destructured."
            }
          }
        }
      ]
    },
    "PatternMapEntry": {
      "type": "object",
      "description": "One entry of a map pattern.",
      "additionalProperties": false,
      "required": [
        "key",
        "pattern"
      ],
      "properties": {
        "key": {
          "$ref": "l1.json#/$defs/Expr",
          "description": "The key."
        },
        "pattern": {
          "$ref": "#/$defs/Pattern",
          "description": "What its value must match."
        }
      }
    }
  }
}
