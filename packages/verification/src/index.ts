// @bridge/verification — Verifier host, first-party verifiers (static + visual at MVP), reference-build manager, report renderer.
//
// BRIDGE-STUB(M2): public surface — runVerifiers(build, config) with the static and visual verifiers. Remaining five verifiers land at M4 (Blueprint §3 M4-T1..T6).
//
// The package exists from M0 so that the architecture rules (Spec §1.2) are enforced from the
// first commit, and so that dependents can be written against a real module boundary.

export {};
